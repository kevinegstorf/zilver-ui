import { registerInstance as __stencil_registerInstance } from "@stencil/core";
import { h } from "@stencil/core";
export const TextInput = class {
    constructor(hostRef) {
        __stencil_registerInstance(this, hostRef);
    }
    handleKeyDown() {
        this.textValue = this.textInput.value;
    }
    render() {
        return (h("input", { type: "text", placeholder: this.placeholder,
            // ref={el => (this.textInput = el)}
            value: this.textValue || this.value }));
    }
    static get style() { return "STYLE_TEXT_PLACEHOLDER:zui-text-input"; }
};
